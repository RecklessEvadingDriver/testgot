[project]
name = "wromgpt"
version = "0.0.0"
description = "FastAPI service for WromGPT"
requires-python = ">=3.11,<3.12"

[tool.setuptools]
py-modules = ["app", "index", "example_client"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
